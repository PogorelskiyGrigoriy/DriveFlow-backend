{
  "name": "driveflow-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev:backend": "npm run dev -w @driveflow/backend",
    "dev:web": "npm run dev -w @driveflow/web-app",
    "build:shared": "npm run build -w @driveflow/shared"
  }
}